{% extends 'base.html.twig' %}
{% block body %}
    <div class="display-6 fst-italic my-3">Dernier Article</div>
    <div class="p-4 p-md-5 mb-4 rounded text-body-emphasis bg-body-secondary">
        <div class="col-lg-6 px-0">
            <h1 class="display-4 fst-italic">{{ latestPost.title }}</h1>
            <p class="lead my-3">{{ latestPost.intro }}</p>
            <p class="lead mb-0"><a href="/php_blog_adventure/posts/{{ latestPost.id }}" class="text-body-emphasis fw-bold">Lire...</a></p>
        </div>
    </div>

            {% include 'home/info.html.twig' %}
            {% include 'home/_contact_form.html.twig' %}

{% endblock %}